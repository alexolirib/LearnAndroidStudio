<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="learnandroidstudio.studio.alexo.learnandroidstudio">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- aqui escolhe qual o main da aplicação -->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AlcoolOuGasolinaActivity" />
        <activity android:name=".AdivinhaNumActivity" />
        <activity android:name=".FrasesDoDiaActivity" />
        <activity android:name=".IdadeCachorroActivity" />
        <activity android:name=".AtmEmpresaConsultoriaActivity" />
        <activity android:name=".EmpresaActivity" />
        <activity android:name=".ServicoActivity" />
        <activity android:name=".ClienteActivity" />
        <activity android:name=".ContatoActivity" />
        <activity android:name=".CaraOuCoroaActivity" />
        <activity android:name=".DetalheCaraOuCoroaActivity"></activity>
    </application>

</manifest>